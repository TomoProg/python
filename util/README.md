# utilの使用方法

utilのモジュールを使用する場合は
.pyファイルの最初に以下を記入してください

import sys
sys.path.append('path')
import 'mojule'

'path'にutilディレクトリの絶対パスまたは相対パスを指定してください。
'mojule'に使用するモジュールを指定してください。
（モジュール名はファイル名から.pyを削除したものです。)


